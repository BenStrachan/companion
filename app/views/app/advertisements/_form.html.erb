  <%= simple_form_for([:app, @advertisement]) do |f| %>

    <div class="form-inputs">
      <%= f.input :title %>
      <%= f.input :description, as: :text, input_html: { rows: 5 } %>
      <%= f.input :city %>
    </div>
    <br>
    <div class="form-actions">
      <%= f.button :submit, class: "btn btn-outline-primary"  %>
    </div>
    <br>
  <% end %>
